---
title: "5 天上手 Claude Code CLI"
subtitle: "每天 20 分鐘，從安裝到交出第一個 AI 協作小專案"
author: "楊政憲"
publisher: "楊政憲"
lang: zh-TW
date: "2026-09-22"
rights: "© 2026 楊政憲. All rights reserved."
subject:
  - "電腦"
  - "程式設計"
  - "人工智慧"
description: "一本每日行動指南：用 5 天、每天約 20 分鐘，跟著固定的三格公式（一個指令 + 一句提示 + 一個驗收），在自己的電腦上用 Claude Code CLI 完成一個小網頁專案。適合第一次接觸 AI 程式助理的新手。"
---

# 關於這本書 {.unnumbered}

**讀完這本書後，你可以用 Claude Code CLI，在自己的電腦上從零完成一個小專案——建立檔案、修改內容、檢查差異、提交版本，最後用一行指令讓它自動幫你做事。**

這不是一本 Claude Code 大全。它只做一件事：陪你用 5 天，每天大約 20 分鐘，把一個叫做「心情小日記」的單頁網頁做出來。你不需要會寫程式，因為程式由 Claude 寫；你要學會的是**怎麼下指令、怎麼看它做了什麼、怎麼驗收**。

## 這本書為什麼存在

很多人第一次打開 Claude Code，會卡在兩個地方：一是不知道第一句話該說什麼，二是不敢讓 AI 改自己電腦裡的檔案。結果只試了一次「幫我寫個 Hello World」，就再也沒打開。

這本書把「開始使用」拆成 5 個小步驟，每一步都有一個你看得到、摸得到的成果。

## 適合誰

- 想開始用 AI 寫程式，但還沒真正用過 Claude Code 的人
- 會開終端機（命令列）、會複製貼上指令的人
- 有 Claude 付費訂閱（Pro 或 Max），或有 Anthropic Console 帳號的人

## 不適合誰

- 已經每天使用 Claude Code、想學進階自動化（Hooks、MCP、子代理）的人
- 完全沒開過終端機的人——建議先花 10 分鐘熟悉「開啟終端機、切換資料夾」這兩件事

## 如何使用

每一天都用同一個固定公式，我叫它**「每日三格」**：

| 格子 | 內容 |
| --- | --- |
| ① 一個指令 | 今天要學的新指令或新按鍵 |
| ② 一句提示 | 今天要對 Claude 說的話 |
| ③ 一個驗收 | 今天結束時，你能親眼確認的成果 |

每天做完三格就停，不求多。書末有一張「5 天全景表」、一份「故障排除表」，以及一個「保底模式」——當天真的卡住時，只做保底模式也算完成。

> **版本說明**：本書以 Claude Code 2.1 版撰寫。Claude Code 更新很快，指令與畫面可能與你看到的略有不同；**遇到差異時，請以 `claude --help` 與官方文件的最新說明為準。**

# 開始之前：三個核心原則 {.unnumbered}

在第 1 天之前，先記住三個原則。這本書所有的步驟都是從這三條長出來的。

## 原則一：先說清楚「完成長什麼樣子」

Claude 很會做事，但它不知道你心中的「好」是什麼。與其說「幫我做個網頁」，不如說「做一個單頁網頁，有一個輸入框和一個儲存按鈕，按下後把文字存在瀏覽器裡」。**描述成果，而不是描述願望。**

## 原則二：小步前進，每一步都驗收

一次只請 Claude 做一件事，做完就看結果。一次要求十件事，出錯時你不知道是哪一件壞掉。

## 原則三：你是審核者，不是旁觀者

Claude 改檔案之前會請你同意。請真的去看它要改什麼。這不是麻煩，而是你學習的最佳時機——每一次審核，你都在學它怎麼思考。

# 第 1 天｜安裝並完成第一次對話

## 本章目標

在你的電腦上裝好 Claude Code，登入，並完成第一次對話。

## 讀者痛點

安裝說明看起來很多，不知道哪一種適合自己；裝好之後打開一個黑色視窗，不知道該做什麼。

## 今日三格

| 格子 | 內容 |
| --- | --- |
| ① 一個指令 | `claude` |
| ② 一句提示 | 「用三句話介紹你自己能幫我做什麼。」 |
| ③ 一個驗收 | 終端機裡出現 Claude 的回覆 |

## 操作步驟

**步驟 1：安裝。** 依你的系統擇一：

- macOS／Linux／WSL：`curl -fsSL https://claude.ai/install.sh | bash`
- Windows PowerShell：`irm https://claude.ai/install.ps1 | iex`
- 已經有 Node.js 18 以上：`npm install -g @anthropic-ai/claude-code`

安裝網址與方式偶爾會調整，**上手前請以官方安裝頁面的最新說明為準**。

**步驟 2：確認安裝成功。** 輸入：

```
claude --version
```

看到版本號就代表裝好了。如果想做完整健康檢查，輸入 `claude doctor`。

**步驟 3：建立練習資料夾。** 這 5 天都在這個資料夾裡練習：

```
mkdir mood-diary
cd mood-diary
```

**步驟 4：啟動並登入。** 輸入 `claude`。第一次啟動時會引導你登入 Claude 帳號，照著畫面完成即可。

**步驟 5：第一次對話。** 在提示符號後面輸入：

> 用三句話介紹你自己能幫我做什麼。

按 Enter，等它回覆。

**步驟 6：離開。** 輸入 `/exit` 就能離開。以斜線開頭的指令叫做「斜線指令」，是給 Claude Code 本身的命令，不是給 AI 的問題。想看所有斜線指令，輸入 `/help`。

## 範例

一個常見的新手錯誤，是第一句話就要求太大：

- 【避免】「幫我做一個完整的日記 App，要有登入、雲端同步、手機版。」
- 【建議】「用三句話介紹你自己能幫我做什麼。」

第一天的目標不是做出東西，而是**確認你和 Claude 之間的通道是通的**。

## 檢查清單

- □ `claude --version` 顯示版本號
- □ 已建立 `mood-diary` 資料夾
- □ 已登入並收到 Claude 的第一個回覆
- □ 知道 `/help` 和 `/exit` 的用途

## 本章小結

今天你只做了一件事：讓 Claude Code 在你的電腦上跑起來。明天，我們要讓它認識你的專案。

# 第 2 天｜讓 Claude 認識你的專案

## 本章目標

請 Claude 建立第一個檔案，並產生一份專案說明書 `CLAUDE.md`，讓它每次啟動都記得這個專案在做什麼。

## 讀者痛點

每次開新對話，都要重新跟 Claude 解釋「我在做什麼、要用什麼語言、有什麼規矩」，很累，也容易漏講。

## 今日三格

| 格子 | 內容 |
| --- | --- |
| ① 一個指令 | `/init` 與 `@檔名` |
| ② 一句提示 | 「建立 index.html，做一個心情小日記的單頁網頁骨架。」 |
| ③ 一個驗收 | 資料夾裡出現 `index.html` 與 `CLAUDE.md` |

## 操作步驟

**步驟 1：在練習資料夾啟動。** 先 `cd mood-diary`，再輸入 `claude`。Claude Code 會以「你啟動它的資料夾」當作專案範圍，所以**先進對資料夾，再啟動**。

**步驟 2：請它建立第一個檔案。** 輸入：

> 建立 index.html，做一個心情小日記的單頁網頁骨架：標題「心情小日記」、一個多行輸入框、一個「儲存」按鈕。先不用做儲存功能。

Claude 會先說明它打算做什麼，接著詢問你是否允許建立檔案。看一眼內容，同意即可（第 3 天會詳細說明權限）。

**步驟 3：產生專案說明書。** 輸入：

```
/init
```

Claude 會讀過資料夾內容，產生一份 `CLAUDE.md`。之後每次在這個資料夾啟動，它都會自動讀這份檔案。

**步驟 4：補上你的規矩。** 打開 `CLAUDE.md`，在最後加上你希望它永遠遵守的事，例如：

```
## 我的規則
- 介面文字一律使用繁體中文
- 只用單一 HTML 檔，不引入外部套件
```

**步驟 5：用 @ 指定檔案。** 想讓 Claude 專注在某個檔案時，在提示裡用 `@` 加檔名：

> 請看 @index.html，用條列說明目前的結構。

## 範例

同一個問題，兩種問法的差別：

- 【避免】「我的網頁有什麼問題？」（它得先猜你說的是哪個檔案）
- 【建議】「看 @index.html，列出三個可以讓畫面更好讀的改進點，先不要改。」

「先不要改」這四個字很重要——它讓 Claude 只提建議，由你決定下一步。

## 檢查清單

- □ 用瀏覽器打開 `index.html`，看到標題、輸入框、按鈕
- □ 資料夾裡有 `CLAUDE.md`
- □ `CLAUDE.md` 裡加上了至少一條你的規則
- □ 用過一次 `@檔名`

## 本章小結

`CLAUDE.md` 是你和 Claude 之間的「工作約定」。寫得越清楚，你每天要重複說明的事就越少。

# 第 3 天｜讓 Claude 動手改檔，而你掌握方向盤

## 本章目標

讓 Claude 為網頁加上真正的儲存功能，並學會三種工作模式、中斷與回復。

## 讀者痛點

不敢讓 AI 改檔案，怕它一次改壞一大片；或反過來，每次都盲目按同意，不知道它改了什麼。

## 今日三格

| 格子 | 內容 |
| --- | --- |
| ① 一個指令 | `Shift+Tab`（切換模式）與 `Esc`（中斷） |
| ② 一句提示 | 「先規劃：讓儲存按鈕把日記存進瀏覽器，重新整理後還在。」 |
| ③ 一個驗收 | 寫一段日記、按儲存、重新整理，文字還在 |

## 三種工作模式

按 `Shift+Tab` 可以在幾種模式之間切換，畫面下方會顯示目前模式：

| 模式 | 什麼時候用 |
| --- | --- |
| 一般模式 | 每次改檔都先問你，最適合新手 |
| 自動接受編輯 | 你已信任這次任務，讓它連續改檔 |
| 規劃模式（Plan） | 只讀、只想，不改任何檔案，先給你計畫 |

## 操作步驟

**步驟 1：切到規劃模式。** 按 `Shift+Tab` 直到畫面顯示規劃模式。

**步驟 2：請它先規劃。** 輸入今日提示。Claude 會讀檔案、提出計畫，但不會動任何東西。

**步驟 3：審核計畫。** 看計畫是否合理。不滿意就直接說：「不要用資料庫，只用瀏覽器內建的儲存。」

**步驟 4：同意後執行。** 同意計畫後，Claude 會開始改檔。每次改檔前，它會秀出「改動前後的差異」請你確認——紅色是刪除，綠色是新增。

**步驟 5：練習中斷。** 如果發現它走錯方向，按一下 `Esc` 立刻停下，然後告訴它正確方向。

**步驟 6：練習回復。** 連按兩下 `Esc`，可以回到先前的某個對話點，重新來過。

**步驟 7：驗收。** 用瀏覽器打開 `index.html`，寫一段文字、按儲存、重新整理頁面，確認文字還在。

## 範例

一個好的「修正方向」訊息，要同時說**哪裡不對**和**要什麼**：

- 【避免】「不對，重做。」
- 【建議】「儲存後沒有顯示提示。請在按下儲存後，於按鈕旁顯示『已儲存』兩秒。」

## 檢查清單

- □ 用過一次規劃模式，並在執行前修改過計畫
- □ 至少看過一次改動差異再按同意
- □ 用 `Esc` 中斷過一次
- □ 網頁的儲存功能通過「重新整理後還在」測試

## 本章小結

權限提示不是障礙，而是你的方向盤。先規劃、再執行、每步驗收——這就是和 AI 協作最安全的節奏。

# 第 4 天｜管理對話：清空、延續與存檔

## 本章目標

學會讓長對話保持清爽，隔天接著做；並用 Git 把今天的成果存成一個版本。

## 讀者痛點

對話越聊越長，Claude 開始忘記前面說過的事；關掉視窗後，不知道怎麼回到昨天的進度；改壞了也沒有「上一版」可以退回。

## 今日三格

| 格子 | 內容 |
| --- | --- |
| ① 一個指令 | `/clear`、`/compact`、`claude -c` |
| ② 一句提示 | 「幫我把目前的專案用 git 初始化並提交第一個版本。」 |
| ③ 一個驗收 | `git log` 看得到你的第一筆提交紀錄 |

## 對話管理四招

| 情境 | 做法 |
| --- | --- |
| 要換一個完全不同的任務 | `/clear`：清空對話，重新開始 |
| 對話太長，但還要繼續同一件事 | `/compact`：把前面的對話壓縮成摘要 |
| 關掉後想接著做最近一次對話 | 啟動時用 `claude -c` |
| 想從歷史對話中挑一個回去 | 啟動時用 `claude -r`，從清單中選 |

## 操作步驟

**步驟 1：接續昨天。** 在 `mood-diary` 資料夾輸入 `claude -c`，確認它記得儲存功能的事。

**步驟 2：把成果存成版本。** 輸入今日提示。Claude 會執行 git 指令並請你確認。沒有安裝 Git 的話，它會告訴你，請先安裝 Git 再繼續。

**步驟 3：自己看一次紀錄。** 在提示前加上驚嘆號 `!`，就能直接執行終端機指令，不用離開 Claude：

```
!git log --oneline
```

**步驟 4：加一個小功能再提交。** 請 Claude「在日記下方顯示今天的日期」，驗收後說「提交這次的修改，說明寫『新增日期顯示』」。

**步驟 5：練習清空。** 輸入 `/clear`，再問「我們剛剛做了什麼？」——它會說不知道。這證明了：**重要的事要寫進 `CLAUDE.md` 或提交紀錄，而不是只留在對話裡。**

## 範例

什麼時候該用 `/clear`，什麼時候該用 `/compact`？

- 剛修完網頁，接下來要寫說明文件 → `/clear`（新任務）
- 修儲存功能修到一半，對話已經很長 → `/compact`（同一任務）

## 檢查清單

- □ 用 `claude -c` 成功接續過對話
- □ `git log` 至少有兩筆提交紀錄
- □ 用 `!` 執行過一次終端機指令
- □ 知道 `/clear` 與 `/compact` 的差別

## 本章小結

對話會消失，版本不會。每完成一個能用的小功能，就提交一次——這是你隨時可以回頭的存檔點。

# 第 5 天｜一行指令，讓 Claude 自動幫你做事

## 本章目標

學會不進入對話畫面，直接用一行指令請 Claude 完成任務，並完成本書的小專案結案。

## 讀者痛點

每次都要打開對話、輸入、等待、離開。有些固定的小事（例如檢查檔案、產生說明），其實可以一行解決。

## 今日三格

| 格子 | 內容 |
| --- | --- |
| ① 一個指令 | `claude -p "提示"` |
| ② 一句提示 | 「讀 index.html，產生一份給使用者看的 README.md。」 |
| ③ 一個驗收 | 資料夾出現 README.md，且 git 已提交 |

## 操作步驟

**步驟 1：第一次非互動執行。** 在 `mood-diary` 資料夾輸入：

```
claude -p "用一句話說明 index.html 是做什麼的"
```

Claude 會直接印出答案後結束，不會進入對話畫面。`-p` 的意思是「印出結果就結束」。

**步驟 2：把檔案內容餵給它。** 可以用管線把內容交給 Claude：

```
cat index.html | claude -p "列出這個網頁的所有按鈕"
```

**步驟 3：讓它產生檔案。** 在對話模式中輸入今日提示，驗收 README.md 的內容後提交。

**步驟 4：取得結構化結果。** 需要把結果交給其他程式處理時，可以要求 JSON 格式輸出：

```
claude -p "列出三個改進建議" --output-format json
```

**步驟 5：專案結案。** 請 Claude：「整理這 5 天做了什麼，寫成 CHANGELOG.md，然後提交。」

## 範例

非互動模式適合「輸入明確、輸出固定」的小事：

- 【建議】「檢查這個檔案有沒有錯字」
- 【建議】「用一句話摘要這份文件」
- 【避免】「幫我設計整個網站」（需要來回討論的事，還是用對話模式）

> **安全提醒**：非互動模式會略過部分確認畫面，請只在你信任的資料夾中使用。

## 檢查清單

- □ 用 `claude -p` 成功取得一次回答
- □ 用管線把檔案內容交給 Claude 過一次
- □ 資料夾中有 README.md 與 CHANGELOG.md
- □ 所有成果都已提交，`git status` 顯示沒有未提交的修改

## 本章小結

恭喜，你完成了！你的 `mood-diary` 資料夾裡現在有一個能用的網頁、一份專案說明書、一份使用說明、一份變更紀錄，以及完整的版本歷史。這就是你的第一個 AI 協作小專案。

# 附錄 A｜5 天全景表 {.unnumbered}

| 天 | ① 指令 | ② 提示重點 | ③ 驗收 |
| --- | --- | --- | --- |
| 1 | `claude` | 自我介紹 | 收到回覆 |
| 2 | `/init`、`@` | 建立網頁骨架 | 有 CLAUDE.md |
| 3 | `Shift+Tab`、`Esc` | 先規劃儲存功能 | 重新整理後還在 |
| 4 | `/clear`、`-c`、`!` | 用 git 提交 | git log 有紀錄 |
| 5 | `claude -p` | 產生 README | 全部已提交 |

# 附錄 B｜故障排除表 {.unnumbered}

| 如果… | 那就… |
| --- | --- |
| 輸入 `claude` 顯示找不到指令 | 關掉終端機重開；仍不行就執行 `claude doctor` 或重新安裝 |
| 登入畫面沒有出現 | 在 Claude Code 裡輸入 `/login` |
| Claude 改錯方向 | 按 `Esc` 中斷，說明「哪裡不對、要什麼」 |
| 改壞了想退回 | 連按兩下 `Esc` 回到先前對話點；或請它「用 git 還原上一個版本」 |
| Claude 忘了前面說的事 | 用 `/compact` 壓縮，或把重點寫進 `CLAUDE.md` |
| 回答太籠統、做不出來 | 追問：「請把每一步改成我今天可以完成的檔案或指令。」 |
| 權限提示太多 | 用 `/permissions` 查看與調整允許的操作，或用 `Shift+Tab` 切換模式 |

# 附錄 C｜保底模式 {.unnumbered}

當天真的很累、時間很少，只做這一件事也算完成：

> 進入 `mood-diary` 資料夾，輸入 `claude -c`，問它：「我們目前做到哪裡？下一步最小的一件事是什麼？」然後只做那一件事。

保底模式的目的，是讓你**不中斷**。連續比完美更重要。

# 附錄 D｜第二週進化迴圈 {.unnumbered}

5 天結束後，用四個問題決定第二週要做什麼：

- **保留**：哪個習慣最有用？（例如先規劃再執行）
- **刪除**：哪個步驟其實不需要？
- **替換**：把「心情小日記」換成你真正想做的小工具。
- **改進**：把你最常重複說的話，寫進 `CLAUDE.md`。

建議第二週的主題：用同樣的每日三格，做一個你工作上真的用得到的小工具。

# 附錄 E｜提示詞樣板 {.unnumbered}

**開始一個新功能：**

> 先不要改檔。請看 @檔名，規劃如何〔功能〕。列出步驟、會改哪些檔案、我要怎麼驗收。

**修正方向：**

> 〔現象〕不對。我要的是〔具體結果〕。請只改〔範圍〕，其他不要動。

**驗收與提交：**

> 列出你剛才改了什麼、我要怎麼測試。測試通過後，提交這次修改，說明寫「〔動詞＋內容〕」。

**卡住時：**

> 請把每一步改成我今天可以完成的檔案或指令。

# 版本資訊 {.unnumbered}

- 書名：5 天上手 Claude Code CLI
- 版本：v1.0
- 撰寫依據：Claude Code 2.1 版
- 下一版預計改進：補充操作畫面截圖、補充 Windows 與 macOS 差異對照、新增「第二週實戰範例」

本書提及的產品名稱、指令與網址僅供教學說明；Claude 與 Claude Code 為 Anthropic 的產品與商標，本書為作者獨立撰寫，與 Anthropic 無隸屬關係。**平台規則、安裝方式與指令細節，請以官方最新說明為準。**
